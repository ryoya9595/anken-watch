# 導入をサポートする人へ（案件ウォッチ）

誰かに設置してあげるとき用の1枚。**渡す前に読む。**

## 何を渡すか

- `anken-watch-kit.zip`
- 解説ページのURL： https://ryoya9595.github.io/anken-watch/
- 「Claude Code（またはCodex）にZipを添付して、ページの導入プロンプトを貼ってください」の一言

相手がClaude CodeかCodexを使えるなら、**導入は本人とAIだけで完結する**設計にしてあります。

## 事前に聞いておくこと

| 聞くこと | なぜ |
|---|---|
| **Claudeのプラン（Pro / Max / Team / 無料）** | 🔴 **無料プランではクラウドルーティンが使えない**＝この仕組みは動きません。最初に確認 |
| 配信ツール（エルメ／Lステップ）を使っているか | 使っているなら、既存アカウントに触らせない念押しが要る |
| 使うAI（Claude Code / Codex） | Codexはブラウザ操作ができないので、画面操作は本人にお願いする流れになる |
| どんな案件を探したいか | 当日AIが聞くが、事前に考えておいてもらうと早い |
| GitHubアカウントを持っているか | 無ければ導入時に作る |

## 🔴 いちばん大事な注意

**エルメ／LステップにつないでいるLINE公式アカウントで、トークンを「再発行」させないこと。**

再発行すると配信ツール側のトークンが無効になり、**お客様への配信が止まります**。
このキットは通知専用アカウントを新規に作る手順ですが、画面が似ているので迷い込みやすいです。

渡すときに一言：

> 通知用のLINEは**新しく作ります**。いま配信に使っているアカウントの設定画面は開かないでください。
> 「再発行」というボタンが見えたら、それは別のアカウントを開いている合図なので、押さずに止まってください。

## つまずきやすいところ（上から順に多い）

| つまずき | 対処 |
|---|---|
| **Claudeが無料プラン** | ルーティンが作れない。Pro以上が必要。**事前確認で防ぐ** |
| **友だち追加を忘れる** | 通知の400エラーはほぼこれ |
| **ルーティンのコネクターを外し忘れる** | 既定でGmail・Drive等が6個付く。案件を見るだけなのに書き込み権限が残る |
| **条件が抽象的** | 「デザイン」だけだと関係ない案件が届く。ツール名・案件の形まで書かせる |
| **条件を変えたのに反映されない** | `routine-prompt.md` を直しただけでは反映されない。**ルーティンの「指示」に貼り直す**必要がある |
| cronのUTC間違い | 日本時間 −9時間。朝8時なら `0 23 * * *` |
| 毎日0件 | 条件が厳しすぎる。1週間様子を見て調整する前提を伝えておく |

## 引き渡し時に伝えること

1. **応募は自分でする。** この仕組みは見つけて知らせるだけ
2. 条件に合う案件が無い日は何も届かない ＝ 届かない日があるのは正常
3. **1週間後に条件を見直す。** 最初から完璧な条件にはならない
4. 早く応募したほうが有利なので、通知は朝に受け取るのがよい
5. 条件を変えたいときはAIに「案件の条件を変えたい」と言えばよい

## 設計のねらい（聞かれたら）

- **全部転送しない。** 新着をそのまま流すと、結局それを読む手間が増える。AIが選んでから届ける
- **1回3件が基準。** 毎朝10件届くと開かなくなる。「応募する価値がある」ものだけ3件のほうが、結果的に応募数は増える
- **ヒアリングを最初にやる。** 何を作るかが決まってから設定に入るほうが、途中で迷わない
- **単価を勝手に決めない。** 実績が少ない人に「安くていい」と言わない。目安を示して本人に決めてもらう
- **サイトを巡回しない。** 検索でURLを集める → **先に重複を外す** → 残りだけ15件まで読む、の順。
  人が手で見て回るのと変わらない量に収める

## 中身の構成

```
Claudeのクラウドルーティン（毎朝）
   ↓ WebSearch でURLを集める
   ↓ seen.py --filter で既知のものを外す（★中身を読む前）
   ↓ WebFetch で残りを読む（最大15件）
   ↓ 条件で選ぶ（AIの判断）
   ↓ send_line.py で送る
   ↓ seen.py --commit（送信が成功したときだけ）
```

- `routine-prompt.md` … 条件と手順。**ここが本体**
- `scripts/seen.py` … 通知済み管理。filter → commit の2段階
- `scripts/send_line.py` … LINE送信（dm-watch と同じもの）

## 保守

- 中身は `AI Agents/anken-watch/`
- コードを直したら `python3 dev/test_seen.py` を通してから配る
- Zipを作り直す： `bash dev/build_zip.sh`
