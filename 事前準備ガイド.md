# 事前準備ガイド（案件ウォッチ）

3つの準備をします。**所要 1時間〜1時間半**。

| 準備するもの | 役割 |
|---|---|
| LINE公式アカウント（新規） | 通知の届き先 |
| GitHub の非公開リポジトリ | キットを置く場所・通知済みの記録 |
| Claude のクラウド環境 | 毎朝動かす場所・トークンの保管 |

AIと一緒に進める場合、画面の操作はAIが進めます。
あなたにお願いするのは **ログイン** と **トークンの貼り付け** だけです。

---

## ⚠️ 最初に必ず読んでください

**いま配信に使っているLINE公式アカウント（エルメ・Lステップにつないでいるもの）は使いません。**

そのアカウントで「チャネルアクセストークン」を発行し直すと、**配信ツールが持っている今のトークンが無効になり、配信が止まることがあります。**

通知専用のアカウントを**新しく無料で作ります**。友だちは**あなた1人だけ**です。

作業中、**「再発行」と書かれたボタンは押さないでください**。押すのは「発行」だけです。

---

## STEP 1｜通知用のLINEを作る（30分）

### 1-1. LINE公式アカウントを作る

1. https://manager.line.biz/ を開く
2. **LINEアカウントでログイン**（ふだんスマホで使っているLINEでOK）
3. 「アカウント作成」→「LINE公式アカウントを作成」
4. 入力する：

| 項目 | 入れるもの |
|---|---|
| アカウント名 | `案件メモ`（あとで変えられます。お客様には見えません） |
| メールアドレス | ふだん使っているもの |
| 業種 | 何でも構いません |

### 1-2. 応答設定を変える

1. 左メニュー「**設定**」→「**応答設定**」
2. **あいさつメッセージ**：オフ
3. **応答メッセージ**：オフ
4. Webhook：この仕組みでは使わないので、どちらでも構いません

### 1-3. チャネルアクセストークンを発行する

1. 左メニュー「**設定**」→「**Messaging API**」→「**Messaging APIを利用する**」
2. **プロバイダー**は**新しく作る**のがおすすめ（名前はお名前や屋号で）
3. プライバシーポリシー・利用規約のURLは空欄でOK →「同意する」→「OK」
4. https://developers.line.biz/console/ を開く
5. いま作ったプロバイダー → チャネル（`案件メモ`）を開く
6. 「**Messaging API設定**」タブ → 一番下「**チャネルアクセストークン（長期）**」の「**発行**」

> 🔴 表示が「**再発行**」なら押さないでください。別のアカウントを開いています。
> ブラウザの戻るボタンで戻り、チャネル名を確認してください。

7. 出てきた文字列を**コピーして一時保存**（パスワード管理ツールやメモ帳に）

> 💡 このトークンに**有効期限はありません**。一度取れば更新は不要です。

### 1-4. あなたのユーザーID

1. 「**チャネル基本設定**」タブの一番下 →「**あなたのユーザーID**」
2. `U` から始まる33文字ほどの文字列をコピー

> ⚠️ 必ず**いま作ったチャネルの画面**から取ってください。別チャネルのIDだと届きません。
> ⚠️ 「チャネルID」「LINE ID」とは別物です。

### 1-5. 自分で友だち追加する（忘れやすい）

1. 「**Messaging API設定**」タブの「**ボットのQRコード**」を表示
2. **スマホのLINE**で読み取って友だち追加

**ここを飛ばすと通知が届きません。** トーク一覧に `案件メモ` が出ていれば成功です。

---

## STEP 2｜GitHub に非公開リポジトリを作る（10分）

1. https://github.com/new を開く
2. 入力する：

| 項目 | 値 |
|---|---|
| Repository name | `anken-watch`（好きな名前でOK） |
| 公開設定 | **Private** |

3. 「Create repository」

キットの配置とpushは、AIがやります。

> 💡 すでに Claude Code で使っているリポジトリがあれば、そこに `anken-watch/` を足す形でも構いません。

---

## STEP 3｜Claude の環境にトークンを登録する（15分）

ルーティンが LINE に送れるように、トークンを Claude の環境に預けます。
**コードにも環境変数にも直接書きません。**

### Pro / Max の場合

1. https://claude.ai/code を開く
2. 環境の設定 → `Default` の編集（または新しい環境 `案件ウォッチ` を作る）
3. 「**API credentials**」→「**Add credential**」

| 項目 | 値 |
|---|---|
| Name | `LINE` |
| Allowed websites | `api.line.me` |
| Header | `Authorization` |
| Prefix | `Bearer` |
| Value | STEP 1-3 のトークン（**ここだけ本人が貼り付け**） |

4. 「**Environment variables**」に追加：

```
LINE_TO_USER_ID=（STEP 1-4 のユーザーID）
```

5. 保存

### Team プランの場合

API credentials が使えないので、Network access を **Custom** にして `api.line.me` を許可し、
環境変数に直接トークンを入れます。

```
LINE_CHANNEL_ACCESS_TOKEN=（STEP 1-3 のトークン）
LINE_TO_USER_ID=（STEP 1-4 のユーザーID）
```

---

## STEP 4｜ルーティンを作る（15分）

> 📋 **実画面の通り**（claude.ai/code のルーティン作成フォーム）

1. 左メニュー「**ルーティン**」→ 右上「**＋ 新しいルーティン**」
2. 「**名前**」…必須。`案件ウォッチ`
3. 「**指示**」…大きなテキスト欄。ここに `kit/routine-prompt.md` の `---` の間を貼る
   （手順書で「プロンプト」と呼んでいるものが、画面では「指示」です）
4. 「指示」欄の**下辺**に2つのボタンが並んでいます。**環境の選択はここ**
   - 左：「**リポジトリを選択**」→ STEP 2 のリポジトリ
   - 右：雲アイコン＋「**Default**」→ STEP 3 の環境
5. 「**トリガーを選択**」→「**スケジュール**」
   - `毎日9:00 JSTに実行` というチップが出るので**クリックして編集**
   - 「**カスタム**」を選ぶと「**cron式**」の入力欄が出ます
   - ⚠️ **cron式はUTCです**（日本時間 −9時間）

| 受け取りたい時刻（日本） | cron |
|---|---|
| 朝7時 | `0 22 * * *` |
| 朝8時 | `0 23 * * *` |
| 朝9時 | `0 0 * * *` |

   - 欄の下に「実行は数分ずつずらされます」と注記があります。**きっかりには来ません**

6. 下部のタブ「**コネクター / 動作 / 通知**」
   - 🔴 **コネクターは既定で6個ほど付いた状態になっています**（Gmail・Google Drive など）。
     この仕組みには不要なので、各チップの「×」で**全部外してください**
     （外さないと、案件を見るだけのルーティンに Gmail や Drive への書き込み権限が付いたままになります）

7. 右下「**作成**」

作成後、「**今すぐ実行**」を押して動作を確認します。

---

## STEP 5｜動作確認（5分）

「今すぐ実行」から数分待って、LINEを見ます。

| 結果 | 意味 |
|---|---|
| 案件が届いた | ✅ 完成です |
| 何も届かない | 条件に合う案件が無かっただけかもしれません。実行履歴で確認します |
| ⚠️ が届いた | 本文に理由が書いてあります |

**実行履歴の見かた**：claude.ai/code → ルーティン → 該当の実行 → ログ

- 「0件なので終了」と出ていれば正常（条件に合うものが無かっただけ）
- LINE送信で `401` → API credentials に `api.line.me` が無い、またはトークンが違う
- LINE送信で `400` → `LINE_TO_USER_ID` が違う、または**友だち追加していない**

**まず1回は届けて確認したい場合**は、AIに「条件をいったん緩めて1件だけ通知してみて」と頼んでください。

---

## 控えメモ

| 名前 | どこで使った |
|---|---|
| チャネルアクセストークン | Claude環境の API credentials（Value） |
| あなたのユーザーID | Claude環境の環境変数 `LINE_TO_USER_ID` |

---

## よくあるつまずき

| 症状 | 原因と対処 |
|---|---|
| 通知が届かない（400） | ほぼ **STEP 1-5 の友だち追加忘れ**。次に多いのが別チャネルのユーザーID |
| 通知が届かない（401） | トークンのコピーが途中で切れています。全選択でコピーし直す |
| 「再発行」しか見当たらない | **別のアカウントを開いています。** 押さずにチャネル名を確認 |
| 毎回0件で終わる | 条件が厳しすぎます。単価の下限を下げる／ジャンルを広げる |
| 関係ない案件が届く | 条件の書き方が抽象的です。`routine-prompt.md` を具体的に（ツール名・案件の形まで） |
| 時刻がずれる | 仕様です。実行は数分ずつずらされます |

## あとから変えたくなったら

| 変えたいこと | どこ |
|---|---|
| 探す案件の条件 | `kit/routine-prompt.md` の表 → ルーティンの「指示」に貼り直す |
| 通知の時刻 | ルーティンのスケジュール（UTC） |
| 1回の件数 | `routine-prompt.md` の「1回に知らせる上限」 |
| 一時停止 | ルーティンを無効化 |
