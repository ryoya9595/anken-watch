# 【AIエージェントへの指示】案件ウォッチの導入

このファイルを読んだAI（Claude Code / Codex など）へ。**あなたが主導して**導入を進めてください。

担当者（人間）にやってもらうのは、原則として次の3つだけです。

1. **質問に答える**（どんな案件を探すか）
2. **ログイン**（LINE・GitHub・claude.ai）
3. **秘密の値の貼り付け**（チャネルアクセストークン）

それ以外——画面の遷移、ボタン操作、入力、ファイル配置、コマンド実行、ルーティン作成——は、使えるツールの範囲で**あなたが行います**。

> ⚠️ **絶対に守ること**
>
> - **確認を取る前に、外部サービスの状態を変えない。** 各STEPの冒頭で「これから○○します。進めてよいですか？」と聞き、「はい」をもらってから進める
> - 🔴 **いま使っているLINE公式アカウント（エルメ／Lステップにつないでいるもの）には触らない。** 通知専用アカウントを**新規に作る**。既存チャネルのトークンを再発行すると、担当者の配信が止まる
> - 🔴 **「再発行」ボタンは押さない。** 押してよいのは、新しく作ったチャネルの「**発行**」だけ
> - **パスワード・2段階認証コードを入力しない。** ログイン画面が出たら担当者に交代する
> - **トークンを扱わない。** 画面に表示されたトークンを、チャットに書き出さない・ファイルに保存しない・コミットしない
> - **クラウドワークスに対して、応募・提案・メッセージ送信などの操作を一切しない**（見るだけ）
> - **サイトを片っ端から巡回しない。** 導入の確認で読むのも数件まで
> - **既存のものを消さない。** `rm -rf` / `mv` を使わない
> - **押す前に止まって聞くもの：** 削除・再発行・課金・リポジトリの公開設定の変更

> 💻 Mac / Windows 共通。コマンドはOSごとに書き分けてあります。

---

## STEP 0｜キットを手に入れる

通常は、利用者から**このページのURL**を渡されて始まります。次のどれかでキットを手に入れてください。

1. **Zipが添付されている** → 解凍する。中の `anken-watch/` がキット本体
2. **添付が無い** → 下のコマンドで取得する
3. **取得できなかった** → 何度も試さず、利用者に**Zipの添付**を頼む

> 💡 ネットワークが制限されている環境（Codex は既定で無効）では、2 が失敗することがあります。
> 許可を求めるプロンプトが出たら利用者に許可してもらい、それでも通らなければ 3 に切り替えてください。
> **添付されたファイルを読む分には、どの環境でも問題なく動きます。**

コマンドで取得する場合：

Mac / Linux
```
cd ~/Desktop && curl -sSL -o anken-watch-kit.zip https://ryoya9595.github.io/anken-watch/anken-watch-kit.zip && unzip -q anken-watch-kit.zip
```
Windows（PowerShell）
```
Set-Location "$HOME\Desktop"; Invoke-WebRequest -Uri https://ryoya9595.github.io/anken-watch/anken-watch-kit.zip -OutFile anken-watch-kit.zip; Expand-Archive anken-watch-kit.zip -DestinationPath .
```

## STEP 1｜全体像を説明し、確認を取る

担当者に伝える：

> クラウドワークスの新着案件から、**あなたの条件に合うものだけ**を毎朝LINEで受け取る仕組みを作ります。
> 新着を全部転送するのではなく、AIが選んでから届けます。届くのは1回に3件ほどです。
>
> まず「どんな案件を探すか」を一緒に決めます。白紙で考えなくて大丈夫です。順番に質問します。
>
> そのあと、通知用のLINEを新しく作り、毎朝動くように設定します。いま配信に使っている公式アカウントには触りません。
>
> お願いするのは、質問への回答・ログイン・トークンの貼り付けだけです。所要は1時間〜1時間半ほどです。
>
> なお、クラウドワークスへの応募や提案は一切しません。見るだけの仕組みです。

> 「まず、どんな案件を探すかを決めるところから始めます。よろしいですか？（はい／いいえ）」

## STEP 2｜自分に何ができるか確認する

次を確認して、結果を担当者に一言で伝える。

| 確認 | 方法 | 使えると何ができるか |
|---|---|---|
| **ブラウザ操作** | ブラウザを操作するツールが使えるか（Claude Code なら `mcp__claude-in-chrome__*`。deferred なら先に読み込む） | LINE・GitHub・claude.ai の画面を**あなたが操作**できる |
| `git` | `git --version` | リポジトリ操作 |
| `gh`（GitHub CLI） | `gh auth status` | リポジトリ作成をコマンドでできる |
| `python3` | `python3 --version`（Windows は `python --version`） | 動作確認 |
| Claudeのプラン | 担当者に聞く（Pro / Max / Team） | Pro/Max なら API credentials が使える |

**ブラウザ操作が使えない場合**（Codex など）：
以降の「画面を進める」STEPは、`事前準備ガイド.md` の該当箇所を**1ステップずつ読み上げて案内**する形に切り替えてください。
担当者が自分で画面を操作し、あなたは「次に何を押すか」「いま何が起きているか」を説明します。
**それでも導入は問題なく完了できます。** 時間が10〜20分ほど延びるだけです。

> 📌 **ネットワークが制限されている環境（Codex など）で進めるとき**
>
> ローカルでの疎通確認（`send_line.py --self-test`）や `git push` が通らないことがあります。
> **どちらも必須ではありません。**
>
> - 疎通確認 … **この仕組みはClaudeのクラウドで動く**ので、STEP 9 の「今すぐ実行」で確認できます。飛ばしてください
> - `git push` … 担当者に自分のターミナルで実行してもらってください
> - `python3 dev/test_seen.py` … ネットワークを使わないので、どの環境でも動きます

ブラウザ操作が使える場合のルール：
- 必ず新しいタブを作り、そのタブだけを操作する。担当者が開いているタブには触らない
- ログイン画面が出たら「ログインをお願いします。終わったら『できた』と言ってください」と伝えて待つ
- **トークンが表示されている画面を読み取らない。** 「画面に出ている文字列をコピーしてください」と伝えるだけにする

## STEP 3｜どんな案件を探すか決める（ここが最重要）

> 「どんな案件を探すか、7つほど質問します。1問ずつ答えてください。」

`案件条件ヒアリングシート.md` を開き、**Q1から順に1問ずつ**聞いてください。

> ⚠️ **一度に全部聞かない。** 「どんな案件が欲しいですか？」と白紙で聞かれても、たいていの人は答えられません。
> シートにある選択肢を提示して、選んでもらってから掘り下げます。

> ⚠️ **単価を勝手に決めない。** 実績が少ない人に「安い案件でいいですね」と言わないこと。
> 目安を示して、**本人に決めてもらいます。**

聞き終わったら、内容を読み上げて確認します：

> 「この条件で探します。合っていますか？
> 　ジャンル：… ／ 単価：… ／ 避けたいもの：… ／ 1回に3件まで ／ 毎朝8時」

ここで合意が取れてから、次に進みます。

## STEP 4｜通知用のLINEを用意する

> 「通知を受け取るLINE公式アカウントを新しく作ります。いま使っているアカウントには触りません。進めてよいですか？」

`事前準備ガイド.md` の STEP 1-1 〜 1-5 の順に進める。要点：

1. https://manager.line.biz/ でアカウント作成（名前は `案件メモ`）。ログインは担当者
2. 応答設定：あいさつメッセージ・応答メッセージを**オフ**
3. 「設定 → Messaging API → Messaging APIを利用する」。**プロバイダーは新規作成**を勧める
4. https://developers.line.biz/console/ →（チャネル）→「Messaging API設定」→ チャネルアクセストークン（長期）の「**発行**」
   - 🔴 表示が「再発行」なら押さない
   - トークンは**読み上げない・書き出さない**。「コピーして一時保存してください」と伝える
5. 「チャネル基本設定」の一番下 →「**あなたのユーザーID**」を控えてもらう
6. QRコードを表示し、スマホで**友だち追加**してもらう
   - ⚠️ ここを飛ばすと通知が届かない。**「追加できました」の返事をもらってから次へ**

## STEP 5｜GitHub に非公開リポジトリを作り、キットを置く

> 「キットを置く非公開リポジトリを作ります。進めてよいですか？」

**`gh` が使える場合（推奨）**

```
mkdir -p ~/anken-watch && cd ~/anken-watch
cp -R "<このキットのパス>" ./anken-watch
git init -b main
git add -A && git commit -m "案件ウォッチを追加"
gh repo create anken-watch --private --source=. --push
```

**既存のリポジトリに足す場合**：そのリポジトリの中に `anken-watch/` をコピーして push（他のファイルには触らない）。

確認：`git ls-files` に次が含まれること。

```
anken-watch/routine-prompt.md
anken-watch/scripts/send_line.py
anken-watch/scripts/seen.py
anken-watch/state/seen.json
```

⚠️ ルーティンのプロンプトは `anken-watch/scripts/...` というパスでスクリプトを呼びます。
**リポジトリ直下に `anken-watch/` フォルダがある形**にしてください（中身をバラさない）。

## STEP 6｜Claude の環境にトークンを登録する

> 「Claudeのクラウド環境にLINEのトークンを登録します。貼り付けだけお願いします。進めてよいですか？」

1. https://claude.ai/code → 環境の設定 → `Default` の編集（または新規環境）
2. **Pro / Max**：「API credentials」→「Add credential」

| 項目 | 値 |
|---|---|
| Name | `LINE` |
| Allowed websites | `api.line.me` |
| Header | `Authorization` |
| Prefix | `Bearer` |
| Value | ← **担当者が貼り付け** |

3. 「Environment variables」に、あなたが入力：
   ```
   LINE_TO_USER_ID=（STEP 4-5 で控えたユーザーID）
   ```
4. **Team の場合**：Network access を Custom にして `api.line.me` を追加し、
   環境変数に `LINE_CHANNEL_ACCESS_TOKEN` と `LINE_TO_USER_ID` を入れる（値の貼り付けは担当者）

## STEP 7｜条件をプロンプトに書き込む（あなたが実行）

`anken-watch/routine-prompt.md` を開き、「**この人が探している案件【ここを埋める】**」の表を、
STEP 3 で聞いた内容に**書き換えます**。

書き方のコツ：

| ❌ 曖昧 | ✅ 具体的 |
|---|---|
| デザイン | バナー制作とYouTubeサムネイル。Canva・Photoshop使用 |
| そこそこの単価 | 固定10,000円以上、または時給1,200円以上。単価非公開は除く |
| 変な案件は除く | テスト作業、コンペ形式、10名以上の大量募集、クライアント実績0件 |

**抽象的に書くと、関係ない案件が届きます。** 固有のツール名・案件の形まで書いてください。

書き換えたら、担当者に該当部分を見せて確認します。

## STEP 8｜ルーティンを作る

> 「毎朝○時に動くルーティンを作ります。進めてよいですか？」

| 項目 | 値 |
|---|---|
| 名前 | `案件ウォッチ` |
| 指示 | `anken-watch/routine-prompt.md` の `---` の間（**STEP 7 で書き換えたもの**） |
| リポジトリ | STEP 5 のもの（push 許可があればON） |
| 環境 | STEP 6 の環境 |
| スケジュール | STEP 3 で決めた時刻のcron（**UTC**。日本時間 −9時間） |
| コネクター | 🔴 **なし（既定で付いてくる分を全部外す）** |

作り方は、使えるものから順に：
1. `/schedule` コマンドが使えるなら、それで作る
2. ブラウザ操作が使えるなら、https://claude.ai/code/routines を開いて入力する
3. どちらも無いなら、上の表とプロンプトをコードブロックで表示して、担当者に作ってもらう

> 📋 **実画面の通り**（claude.ai/code のルーティン作成フォーム）
> - 「指示」欄の**下辺**に「リポジトリを選択」と雲アイコン「Default」が並んでいる。**環境の選択はここ**
> - トリガーは「スケジュール」→ 出てきたチップを**クリックして編集**→「カスタム」で cron 入力欄
> - ⚠️ **cron式はUTC解釈**
> - 🔴 下部タブの「コネクター」に**既定で6個ほど付いている**（Gmail・Drive など）。**全部×で外す**
> - 右下「作成」

作成後、「**今すぐ実行**」を押します。

## STEP 9｜初回実行の確認

数分待って、担当者にLINEを確認してもらいます。

| 結果 | 見かた |
|---|---|
| 案件が届いた | ✅ 完了 |
| 何も届かない | 条件に合う案件が無かっただけの可能性。実行履歴を見る |
| ⚠️ が届いた | 本文の理由を読む |

実行履歴（claude.ai/code）で見るところ：

- 「0件なので終了」→ 正常。条件に合うものが無かっただけ
- LINE送信で `401` → API credentials に `api.line.me` が無い、またはトークンが違う
- LINE送信で `400` → `LINE_TO_USER_ID` が違う、または**友だち追加していない**

**1回は届くところを見せたい場合**は、条件を一時的に緩めて（単価の下限を下げる等）再実行し、
確認できたら元に戻します。担当者に「いま一時的に条件を緩めます」と伝えてから行ってください。

## STEP 10｜完了を伝える

> 「導入できました。毎朝○時に、条件に合う案件だけが届きます。合うものが無い日は何も届きません。
>
> **1週間使ってみて、調整しましょう。**
> ・届きすぎる → 条件を厳しく（単価を上げる／ジャンルを絞る）
> ・届かない → 条件を緩める（単価を下げる／ジャンルを広げる／「未経験可」を含める）
> ・合わない案件が来る → 条件の書き方を具体的にする
>
> 条件を変えたくなったら、私に『案件の条件を変えたい』と言ってください。」

担当者に、**1週間後に見直す**ことをカレンダーに入れてもらうと確実です。

---

## 変更・停止（担当者に頼まれたら）

| 変えたいこと | どこ |
|---|---|
| 探す条件 | `anken-watch/routine-prompt.md` の表 → **ルーティンの「指示」に貼り直す**（ファイルを直すだけでは反映されない） |
| 通知の時刻 | ルーティンのスケジュール（UTC） |
| 1回の件数 | `routine-prompt.md` の「1回に知らせる上限」 |
| 対象サイトを増やす | `routine-prompt.md` の検索例にランサーズ等を足す。`seen.py` は主要3サイトのURLに対応済み |
| 停止 | ルーティンを無効化（削除は担当者の明確な指示があるときだけ） |

**LINE公式アカウント・チャネル・リポジトリ・ルーティンは、担当者から明確に「消して」と言われない限り消さない。**

## コードを直したとき

```
python3 dev/test_seen.py
```

通知済み管理（URLの正規化・2段階の確定・古い記録の間引き）を検証します。
ネットにもLINEにも接続しないので、いつでも実行できます。

> `seen.py` の **filter → commit の2段階**は、送信に失敗した回の案件を次回もう一度通知するための仕組みです。
> 「送信前に commit する」形に変えると、**失敗した回の案件が二度と通知されなくなります**。この順序を崩さないでください。
